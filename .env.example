# ====================================
# MONGODB - Base de Datos
# ====================================
# Usa la conexión de MongoDB Atlas que ya tienes configurada en Vercel
MONGODB_URL="mongodb+srv://usuario:password@cluster.mongodb.net/?retryWrites=true&w=majority"
DATABASE_NAME="cuentas_por_pagar"

# ====================================
# JWT - Autenticación
# ====================================
# Genera con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="CHANGE-THIS-TO-A-SECURE-RANDOM-KEY-MIN-32-CHARACTERS-LONG"
JWT_ALGORITHM="HS256"
JWT_EXPIRATION="7d"

# ====================================
# VERCEL BLOB STORAGE - Archivos
# ====================================
# OPCIONAL en desarrollo (usa public/uploads si no está definido)
# REQUERIDO en producción (Vercel lo genera automáticamente al conectar Blob Store)
# Obtén tu token de: https://vercel.com/dashboard/stores
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxxxxxxxxxx"

# ====================================
# NEXTAUTH (Legacy - puede removerse si no se usa)
# ====================================
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="CHANGE-THIS-TO-ANOTHER-SECURE-RANDOM-KEY-32-CHARS"

# ====================================
# CONFIGURACIÓN DE UPLOADS (Legacy - ahora usa Blob)
# ====================================
# Estas variables son para compatibilidad local cuando no hay BLOB_READ_WRITE_TOKEN
UPLOAD_DIR="./public/uploads"
MAX_FILE_SIZE_MB="10"

# ====================================
# ENVIRONMENT
# ====================================
NODE_ENV="development"
PORT="3000"

# API URL (para client-side fetching)
NEXT_PUBLIC_API_URL="http://localhost:3000"

# ====================================
# NOTAS IMPORTANTES
# ====================================
# 1. DESARROLLO LOCAL: Si no defines BLOB_READ_WRITE_TOKEN, usa file system (public/uploads/)
# 2. PRODUCCIÓN VERCEL: Vercel genera BLOB_READ_WRITE_TOKEN automáticamente al conectar Blob Store
# 3. NUNCA subas .env.local con valores reales a Git
# 4. En Vercel configura las variables en: Dashboard > Settings > Environment Variables

